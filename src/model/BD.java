package model;

public class BD extends Livre {
    public BD(String auteur, String titre, long id, float tarif) {
        super(auteur, titre, id, tarif);
    }
    public BD() {
        super();
    }
}
